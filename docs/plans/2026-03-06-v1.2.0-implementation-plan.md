# v1.2.0 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Lua プラグイン安定性改善、検索フィルタ修正、バッチ現像処理、バージョン同期、NDJSON ストリーミング
**Architecture:** Lua チャンク分割 + NDJSON ストリーミング + setSelectedPhotos バッチ現像 + ビルド時バージョン注入
**Tech Stack:** Lua (Lightroom SDK), Python 3.10+, Click, pytest
---

## 設計書参照

`docs/plans/2026-03-06-v1.2.0-stability-and-batch-design.md`

## 前提条件

- 下位互換は不要（ユーザー1人）
- テスト: `venv/bin/python -m pytest tests/ -v --ignore=tests/e2e`
- Lint: `ruff check . && ruff format --check .`
- venv: `venv/bin/python`
- MockLightroomServer ベースのテスト

---

## Phase 1: クリティカル修正

### Task 1: findPhotos フィルタ拡張 — テスト作成

**Files:**
- Create: `tests/test_catalog_filters.py`

**Step 1: テストを書く**
`tests/test_catalog_filters.py` を作成し、以下のテストケースを定義:
- `test_find_photos_with_folder_path_filter`: `folderPath` フィルタで部分一致する写真のみ返ること
- `test_find_photos_with_capture_date_from_filter`: `captureDateFrom` 以降の写真のみ返ること
- `test_find_photos_with_capture_date_to_filter`: `captureDateTo` 以前の写真のみ返ること
- `test_find_photos_with_file_format_filter`: `fileFormat` の完全一致フィルタ
- `test_find_photos_with_keyword_filter`: `keyword` の部分一致フィルタ
- `test_find_photos_with_filename_filter`: `filename` の部分一致フィルタ
- `test_find_photos_with_combined_filters`: 複合フィルタ（rating + flag + fileFormat）
- `test_find_photos_with_unknown_filter_key`: 不明なフィルタキーで `warnings` フィールドが返ること
- `test_find_photos_with_invalid_rating_value`: 不正な rating 値でエラーレスポンス

MockLightroomServer を使い、`catalog.findPhotos` コマンドのレスポンスを登録して CLI 経由でテスト。

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_catalog_filters.py -v`
Expected: テストが定義されていることを確認（実装前なので FAIL は想定内）

**Step 3: コミット**
```
git commit -m "test: findPhotos フィルタ拡張のテスト追加 (Phase 1 準備)"
```

---

### Task 2: findPhotos フィルタ拡張 — Lua 実装

**Files:**
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`

**Step 1: 実装**
`CatalogModule.findPhotos` 関数（L560-661）を修正:
- フィルタ順序を最適化: 軽いフィルタ（rating, flag, colorLabel, fileFormat）を先に、重いフィルタ（keyword, folderPath, filename）を後に適用
- 追加フィルタの実装:
  - `searchDesc.folderPath`: `photo:getRawMetadata("path")` で `string.find` 部分一致
  - `searchDesc.captureDateFrom`: `photo:getFormattedMetadata("dateTimeOriginal")` と ISO 8601 文字列比較（`>=`）
  - `searchDesc.captureDateTo`: 同上（`<=`）
  - `searchDesc.fileFormat`: `photo:getRawMetadata("fileFormat")` の完全一致
  - `searchDesc.keyword`: `photo:getRawMetadata("keywords")` をイテレートし、`keyword:getName()` で部分一致
  - `searchDesc.filename`: `photo:getFormattedMetadata("fileName")` で `string.find` 部分一致
- 不明なフィルタキーを検出し、レスポンスの `warnings` フィールドに列挙
- 不正な値のバリデーション（rating が数値でない場合等）でエラーレスポンスを即時返却

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_catalog_filters.py -v`
Expected: PASS

**Step 3: コミット**
```
git commit -m "feat: findPhotos にフィルタ拡張を実装 (folderPath, captureDate, fileFormat, keyword, filename)"
```

---

### Task 3: searchPhotos を findPhotos に委譲

**Files:**
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`
- Create: `tests/test_search_compat.py`

**Step 1: テストを書く**
`tests/test_search_compat.py` を作成:
- `test_search_photos_delegates_to_find_photos`: `searchPhotos` の呼び出しが `findPhotos` と同じ結果を返すこと
- `test_search_photos_deprecated_log`: `searchPhotos` が deprecated ログを出力すること（MockLightroomServer で確認可能な範囲）

**Step 2: 実装**
`CatalogModule.searchPhotos` 関数（L52-509）を以下に置換:
- `criteria` パラメータを `searchDesc` フォーマットに変換
- `CatalogModule.findPhotos` を呼び出して委譲
- `logger:warn("deprecated: searchPhotos は findPhotos に統合されました。次バージョンで削除予定。")` をログ出力

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_search_compat.py tests/test_catalog_filters.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "refactor: searchPhotos を findPhotos に委譲 (deprecated 化)"
```

---

### Task 4: SKILL.md から searchPhotos を削除

**Files:**
- Modify: `plugin/skills/lightroom-cli/SKILL.md`

**Step 1: 実装**
SKILL.md 内の `searchPhotos` 関連の記述を削除し、`findPhotos` のドキュメントを拡充:
- 新しいフィルタパラメータ（`folderPath`, `captureDateFrom`, `captureDateTo`, `fileFormat`, `keyword`, `filename`）を追加
- 使用例を更新

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/ -v --ignore=tests/e2e -k "skill or mcp_instructions"`
Expected: PASS（SKILL.md の整合性テストがあれば）

**Step 3: コミット**
```
git commit -m "docs: SKILL.md から searchPhotos 削除、findPhotos のフィルタドキュメント拡充"
```

---

### Task 5: batchApplySettings に setSelectedPhotos を追加 — テスト作成

**Files:**
- Create: `tests/test_develop_batch.py`

**Step 1: テストを書く**
`tests/test_develop_batch.py` を作成:
- `test_batch_apply_settings_multiple_photos`: 3枚の写真に設定を適用し、全枚の結果が返ること
- `test_batch_apply_settings_partial_failure`: 5枚中2枚が見つからない場合、3枚は成功し部分結果が返ること
- `test_batch_apply_settings_max_50_limit`: 51枚指定でエラーが返ること
- `test_batch_apply_settings_empty_photo_ids`: 空の photoIds でエラー
- `test_batch_set_value_single_param`: 新コマンド `develop.batchSetValue` で1パラメータを複数写真に適用
- `test_batch_set_value_max_50_limit`: 51枚で制限エラー

MockLightroomServer にレスポンスを登録してテスト。

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_develop_batch.py -v`
Expected: テスト定義確認（実装前なので一部 FAIL は想定内）

**Step 3: コミット**
```
git commit -m "test: バッチ現像処理のテスト追加 (batchApplySettings, batchSetValue)"
```

---

### Task 6: batchApplySettings の修正 — Lua 実装

**Files:**
- Modify: `lightroom_sdk/plugin/DevelopModule.lua`

**Step 1: 実装**
`DevelopModule.batchApplySettings` 関数（L616-721）を修正:
- バッチサイズ制限: `#photoIds > 50` の場合、エラーレスポンスを返す
- 処理フロー変更:
  1. 現在の選択状態を保存: `catalog:getTargetPhoto()`, `catalog:getTargetPhotos()`
  2. `LrApplicationView.getCurrentModuleName()` で現在のモジュール確認
  3. Develop モジュールでない場合: `LrApplicationView.switchToModule("develop")` + `LrTasks.sleep(0.5)`
  4. 各 photoId のループ内で: `catalog:setSelectedPhotos(photo, {photo})` → `LrTasks.sleep(0.1)` → `LrDevelopController.setValue()` を各パラメータに適用
  5. 元の選択状態を復元
- `LrApplicationView` のインポートを `ensureLrModules()` に追加

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_develop_batch.py -v`
Expected: PASS

**Step 3: コミット**
```
git commit -m "fix: batchApplySettings に setSelectedPhotos を追加し写真ごとに設定適用"
```

---

### Task 7: develop.batchSetValue 新コマンド — Lua 実装

**Files:**
- Modify: `lightroom_sdk/plugin/DevelopModule.lua`
- Modify: `lightroom_sdk/plugin/PluginInit.lua` (コマンド登録)

**Step 1: 実装**
`DevelopModule.batchSetValue` 関数を新規追加:
- パラメータ: `photoIds` (number[]), `param` (string), `value` (number)
- バッチサイズ制限: 50枚
- `batchApplySettings` と同じ `setSelectedPhotos` + `setValue` パターンを使用
- レスポンス: `{ results: [{photoId, success, error?}], processed, succeeded }`

`PluginInit.lua` のコマンド登録に `develop.batchSetValue` を追加。

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_develop_batch.py -v`
Expected: PASS

**Step 3: コミット**
```
git commit -m "feat: develop.batchSetValue コマンド追加 (1パラメータ x 複数写真)"
```

---

### Task 8: バッチ現像の動的タイムアウト — Python 実装

**Files:**
- Modify: `lightroom_sdk/retry.py`
- Modify: `tests/test_retry.py`

**Step 1: テストを書く**
`tests/test_retry.py` に追加:
- `test_calculate_batch_timeout_10_photos`: 10枚 → 30s
- `test_calculate_batch_timeout_50_photos`: 50枚 → 110s
- `test_calculate_batch_timeout_1_photo`: 1枚 → 30s（最小値）
- `test_calculate_batch_timeout_exceeds_max`: 100枚 → 120s（最大値）
- `test_batch_command_timeouts_registered`: `develop.batchApplySettings` と `develop.batchSetValue` の COMMAND_TIMEOUTS エントリ確認

**Step 2: 実装**
`lightroom_sdk/retry.py` に追加:
- `calculate_batch_timeout(photo_count: int) -> float` 関数: `min(120.0, max(30.0, 10.0 + 2.0 * photo_count))`
- `COMMAND_TIMEOUTS` に以下を追加:
  - `"catalog.findPhotos": 90.0`
  - `"catalog.getAllPhotos": 90.0` (60→90)
  - `"catalog.getCollections": 60.0`
  - `"develop.batchApplySettings": 120.0` (フォールバック用の固定値)
  - `"develop.batchSetValue": 120.0`

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_retry.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: バッチ現像の動的タイムアウト計算を追加 (calculate_batch_timeout)"
```

---

### Task 9: CLI にバッチ現像コマンドを追加

**Files:**
- Modify: `cli/commands/develop.py`
- Modify: `tests/integration/test_cli_develop.py`

**Step 1: テストを書く**
`tests/integration/test_cli_develop.py` に追加:
- `test_cli_develop_batch_set`: `lr develop batch-set --photo-ids 1,2,3 Exposure 0.5` の CLI テスト
- `test_cli_develop_batch_set_exceeds_limit`: 51枚指定時のバリデーションエラー

**Step 2: 実装**
`cli/commands/develop.py` に `batch-set` コマンドを追加:
- `--photo-ids` オプション: カンマ区切りの写真 ID リスト
- `PARAM` と `VALUE` 引数
- `calculate_batch_timeout` で動的タイムアウトを計算
- `execute_command(ctx, "develop.batchSetValue", {...}, timeout=dynamic_timeout)` を呼び出し

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/integration/test_cli_develop.py -v -k "batch"`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: CLI に develop batch-set コマンドを追加"
```

---

## Phase 2: 安定性改善

### Task 10: Lua チャンク分割処理 — CatalogModule

**Files:**
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`

**Step 1: 実装**
`CatalogModule.findPhotos` と `CatalogModule.searchPhotos`（委譲先）を改修:
- チャンク定数を定義: `FILTER_CHUNK_SIZE = 50`, `METADATA_CHUNK_SIZE = 50`, `DEFAULT_PAGE_SIZE = 500`, `MAX_PAGE_SIZE = 2000`
- `withReadAccessDo` をチャンク単位で分割:
  1. `catalog:getAllPhotos()` で ID 配列を一括取得（軽量なので分割不要）
  2. フィルタリングを 50 枚ずつ `withReadAccessDo` で実行し、各チャンク間で `LrTasks.yield()`
  3. メタデータ取得も 50 枚ずつ `withReadAccessDo` で分割
- `limit` のデフォルトを 500 に変更、最大 2000 に制限
- チャンクエラー発生時: スキップして次チャンクへ、`partialErrors` に記録

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_catalog_filters.py tests/test_search_compat.py -v`
Expected: PASS（既存テストがデグレしないこと）

**Step 3: コミット**
```
git commit -m "feat: CatalogModule にチャンク分割処理を実装 (withReadAccessDo 分割 + yield)"
```

---

### Task 11: getCollections の最適化

**Files:**
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`

**Step 1: 実装**
`CatalogModule.getCollections` 関数（L665-693）を改修:
- `#collection:getPhotos()` を削除し、`photoCount` フィールドは省略（遅延取得に変更）
- または `collection:getPhotoCount()` が LR SDK に存在する場合はそれを使用
- コレクションの走査にも `LrTasks.yield()` を挿入（大量コレクション対策）

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/ -v --ignore=tests/e2e -k "collection"`
Expected: PASS

**Step 3: コミット**
```
git commit -m "fix: getCollections の photoCount 計算を最適化 (遅延取得 + yield)"
```

---

### Task 12: コマンドキャンセレーション機構

**Files:**
- Modify: `lightroom_sdk/plugin/CommandRouter.lua`
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`

**Step 1: 実装**
**CommandRouter.lua:**
- `commandStartTime` テーブルを追加: コマンド実行開始時に `requestId → os.clock()` を記録
- `cancelledRequests` テーブルを追加: キャンセルされた requestId を記録
- `LUA_COMMAND_TIMEOUT` テーブル: コマンド種別ごとのタイムアウト（デフォルト 80s, preview系 110s）
- `isTimedOut(requestId, command)` ヘルパー関数: 経過時間チェック
- `isCancelled(requestId)` ヘルパー関数: キャンセルテーブル参照
- receiver チャネルの受信ループに `cancelCommand` メッセージの処理を追加

**CatalogModule.lua:**
- チャンク処理ループの先頭で `CommandRouter:isTimedOut()` と `CommandRouter:isCancelled()` をチェック
- タイムアウト/キャンセル時: 処理済みチャンクの部分結果 + `incomplete: true` + `reason: "timeout"` を返却

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/ -v --ignore=tests/e2e`
Expected: PASS

**Step 3: コミット**
```
git commit -m "feat: Lua 側コマンドキャンセレーション機構 (タイムアウト自己検知 + receiver 経由通知)"
```

---

### Task 13: incomplete フラグと部分成功レスポンス — テスト + 実装

**Files:**
- Create: `tests/test_incomplete_response.py`
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`

**Step 1: テストを書く**
`tests/test_incomplete_response.py` を作成:
- `test_incomplete_flag_on_timeout`: タイムアウト時のレスポンスに `incomplete: true` と `reason: "timeout"` が含まれること
- `test_incomplete_flag_on_chunk_error`: チャンクエラー時に `incomplete: true` と `reason: "chunk_errors"` が含まれること
- `test_incomplete_flag_with_partial_errors`: `partialErrors` フィールドにスキップされた写真の情報が含まれること
- `test_processed_count_and_total_count`: `processedCount` / `totalCount` が正しいこと

MockLightroomServer でタイムアウトをシミュレートするレスポンスを登録。

**Step 2: 実装**
`CatalogModule.lua` の findPhotos / getAllPhotos のレスポンス構築を修正:
- チャンクスキップ発生時: `incomplete = true`, `reason = "chunk_errors"`, `partialErrors` リスト
- タイムアウト中断時: `incomplete = true`, `reason = "timeout"`
- 常に `processedCount` / `totalCount` を含める

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_incomplete_response.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: 部分成功時の incomplete フラグと partialErrors レスポンス実装"
```

---

## Phase 3: NDJSON ストリーミング

### Task 14: NDJSON ストリーミング — Python SDK 受信・集約

**Files:**
- Modify: `lightroom_sdk/socket_bridge.py`
- Modify: `lightroom_sdk/client.py`
- Create: `tests/test_ndjson_streaming.py`

**Step 1: テストを書く**
`tests/test_ndjson_streaming.py` を作成:
- `test_ndjson_data_events_aggregated`: 複数の `data` イベントが集約されて単一レスポンスになること
- `test_ndjson_progress_events_received`: `progress` イベントがコールバックで受信されること
- `test_ndjson_final_event_triggers_response`: `final` イベント受信後にレスポンスが返ること
- `test_ndjson_error_event_recorded`: `error` イベントが記録され、final 後にレスポンスに含まれること
- `test_ndjson_request_id_filtering`: 異なる requestId のイベントが混在しても正しくフィルタリングされること

MockLightroomServer の `send_event` を使って NDJSON イベントをシミュレート。

**Step 2: 実装**
**socket_bridge.py:**
- `_pending_streams` ディクショナリを追加: `requestId → { "chunks": [], "errors": [], "final": None }`
- `_receive_loop` で `type` フィールドを持つメッセージを NDJSON ストリーミングイベントとして処理
- `data` → chunks に追記、`progress` → コールバック、`final` → Future を resolve、`error` → errors に追記

**client.py:**
- `execute_command` でストリーミングレスポンスを集約して返却するロジックを追加
- 既存の単一レスポンスパスは変更なし（NDJSON は sender チャネル経由のイベントとして受信）

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_ndjson_streaming.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: Python SDK に NDJSON ストリーミング受信・集約機能を実装"
```

---

### Task 15: NDJSON ストリーミング — Lua 送信側

**Files:**
- Modify: `lightroom_sdk/plugin/CatalogModule.lua`
- Modify: `lightroom_sdk/plugin/CommandRouter.lua`

**Step 1: 実装**
**CommandRouter.lua:**
- `sendStreamEvent(requestId, eventType, payload)` ヘルパー関数を追加:
  - sender チャネル経由で `{ requestId, type, chunkIndex, payload }` を送信
  - `chunkIndex` はコマンドごとのカウンターで管理

**CatalogModule.lua:**
- `findPhotos` / `getAllPhotos` のチャンク処理で、各チャンク完了時に:
  - `CommandRouter:sendStreamEvent(requestId, "data", { photos = chunkPhotos })` を送信
  - `CommandRouter:sendStreamEvent(requestId, "progress", { processed = N, total = M })` を送信
- 全チャンク完了後に `CommandRouter:sendStreamEvent(requestId, "final", { totalPhotos = M, ... })` を送信
- 従来の callback によるレスポンスは final イベント送信後に省略（NDJSON モードの場合）

**Step 2: 検証**
Run: `venv/bin/python -m pytest tests/test_ndjson_streaming.py tests/test_catalog_filters.py -v`
Expected: PASS

**Step 3: コミット**
```
git commit -m "feat: Lua 側 NDJSON ストリーミングイベント送信を実装"
```

---

## Phase 4: バージョン同期と仕上げ

### Task 16: sync_version.py スクリプト作成

**Files:**
- Create: `scripts/sync_version.py`
- Create: `tests/test_version_sync.py`

**Step 1: テストを書く**
`tests/test_version_sync.py` を作成:
- `test_sync_version_updates_init_py`: `pyproject.toml` のバージョンが `__init__.py` に反映されること
- `test_sync_version_updates_plugin_init_lua`: `PluginInit.lua` の `version` が更新されること
- `test_sync_version_updates_info_lua`: `Info.lua` の `VERSION` テーブルが更新されること
- `test_sync_version_idempotent`: 同じバージョンで2回実行しても問題ないこと

**Step 2: 実装**
`scripts/sync_version.py` を作成:
1. `pyproject.toml` から `version` を読み取り（`tomllib` 使用、Python 3.11+ / `tomli` フォールバック）
2. `lightroom_sdk/__init__.py` の `__version__ = "x.x.x"` を正規表現で置換
3. `lightroom_sdk/plugin/PluginInit.lua` の `version = "x.x.x"` を正規表現で置換
4. `lightroom_sdk/plugin/Info.lua` の `VERSION = { major=X, minor=Y, revision=Z, build=1 }` を正規表現で置換
5. 変更があったファイルのパスを stdout に出力

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_version_sync.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: scripts/sync_version.py でバージョン一元管理を実現"
```

---

### Task 17: check_version_sync.py スクリプト作成

**Files:**
- Create: `scripts/check_version_sync.py`

**Step 1: テストを書く**
`tests/test_version_sync.py` に追加:
- `test_check_version_sync_all_match`: 全ファイルが一致する場合 exit code 0
- `test_check_version_sync_mismatch`: 不一致がある場合 exit code 1 + 不一致箇所の出力

**Step 2: 実装**
`scripts/check_version_sync.py` を作成:
1. `pyproject.toml` の `version` を読み取り
2. `lightroom_sdk/__init__.py` の `__version__` と比較
3. `lightroom_sdk/plugin/PluginInit.lua` の `version` と比較
4. `lightroom_sdk/plugin/Info.lua` の `VERSION` と比較
5. 不一致がある場合: 不一致箇所を表示して `sys.exit(1)`
6. 全一致: `sys.exit(0)`

**Step 3: 検証**
Run: `venv/bin/python -m pytest tests/test_version_sync.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: scripts/check_version_sync.py で CI バージョン不整合検出"
```

---

### Task 18: CI にバージョンチェックステップを追加

**Files:**
- Modify: `.github/workflows/test.yml`

**Step 1: 実装**
`.github/workflows/test.yml` に `check-version` ステップを追加:
```yaml
- name: Check version sync
  run: python scripts/check_version_sync.py
```

テストステップの前に配置し、バージョン不整合時に早期 fail させる。

**Step 2: 検証**
Run: `venv/bin/python scripts/check_version_sync.py`
Expected: 現在は不整合（Lua 1.0.0 vs Python 1.1.0）なので fail。Task 16 の sync_version.py を実行後に pass することを確認。

**Step 3: コミット**
```
git commit -m "ci: CI にバージョン同期チェックステップを追加"
```

---

### Task 19: バージョン同期を実行 + ping レスポンス改善

**Files:**
- Modify: `lightroom_sdk/__init__.py`
- Modify: `lightroom_sdk/plugin/PluginInit.lua`
- Modify: `lightroom_sdk/plugin/Info.lua`
- Create: `tests/test_ping_compat.py`

**Step 1: テストを書く**
`tests/test_ping_compat.py` を作成:
- `test_ping_response_has_version`: ping レスポンスに `version` フィールドが存在すること
- `test_ping_response_has_protocol_version`: ping レスポンスに `protocolVersion` フィールドが存在すること
- `test_ping_version_matches_pyproject`: `version` が `pyproject.toml` の値と一致すること

**Step 2: 実装**
1. `venv/bin/python scripts/sync_version.py` を実行してバージョンを同期
2. `PluginInit.lua` の `system.ping` レスポンス（L199-207）に `protocolVersion = "1"` を追加
3. `version` フィールドはそのまま維持（sync_version.py で正しい値に更新済み）

**Step 3: 検証**
Run: `venv/bin/python scripts/check_version_sync.py && venv/bin/python -m pytest tests/test_ping_compat.py -v`
Expected: PASS

**Step 4: コミット**
```
git commit -m "feat: バージョン同期実行 + ping レスポンスに protocolVersion 追加"
```

---

### Task 20: 全体リグレッションテスト + Lint チェック

**Files:**
- (変更なし — 検証のみ)

**Step 1: Lint チェック**
Run: `ruff check . && ruff format --check .`
Expected: PASS（エラーがあれば修正）

**Step 2: 全テスト実行**
Run: `venv/bin/python -m pytest tests/ -v --ignore=tests/e2e`
Expected: 全 PASS

**Step 3: バージョン同期チェック**
Run: `venv/bin/python scripts/check_version_sync.py`
Expected: PASS

**Step 4: コミット（必要な修正があった場合のみ）**
```
git commit -m "fix: v1.2.0 リグレッション修正"
```

---

## タスク依存関係

```
Phase 1 (クリティカル修正):
  Task 1 → Task 2 → Task 3 → Task 4  (検索フィルタ系)
  Task 5 → Task 6 → Task 7 → Task 8 → Task 9  (バッチ現像系)

Phase 2 (安定性改善):
  Task 2 → Task 10  (チャンク分割は findPhotos 修正後)
  Task 10 → Task 11  (getCollections 最適化)
  Task 10 → Task 12  (キャンセレーションはチャンク分割後)
  Task 12 → Task 13  (incomplete フラグはキャンセレーション後)

Phase 3 (NDJSON):
  Task 10 + Task 13 → Task 14  (NDJSON は チャンク分割 + incomplete フラグ後)
  Task 14 → Task 15  (Lua 送信は Python 受信後)

Phase 4 (バージョン同期):
  Task 16 → Task 17 → Task 18 → Task 19  (順序依存)
  Task 19 → Task 20  (全体検証は最後)
```

## 変更ファイル一覧

| ファイル | 操作 | タスク |
|---|---|---|
| `lightroom_sdk/plugin/CatalogModule.lua` | Modify | 2, 3, 10, 11, 12, 13, 15 |
| `lightroom_sdk/plugin/DevelopModule.lua` | Modify | 6, 7 |
| `lightroom_sdk/plugin/PluginInit.lua` | Modify | 7, 19 |
| `lightroom_sdk/plugin/CommandRouter.lua` | Modify | 12, 15 |
| `lightroom_sdk/plugin/Info.lua` | Modify | 19 |
| `lightroom_sdk/retry.py` | Modify | 8 |
| `lightroom_sdk/socket_bridge.py` | Modify | 14 |
| `lightroom_sdk/client.py` | Modify | 14 |
| `lightroom_sdk/__init__.py` | Modify | 19 |
| `cli/commands/develop.py` | Modify | 9 |
| `plugin/skills/lightroom-cli/SKILL.md` | Modify | 4 |
| `.github/workflows/test.yml` | Modify | 18 |
| `scripts/sync_version.py` | Create | 16 |
| `scripts/check_version_sync.py` | Create | 17 |
| `tests/test_catalog_filters.py` | Create | 1 |
| `tests/test_search_compat.py` | Create | 3 |
| `tests/test_develop_batch.py` | Create | 5 |
| `tests/test_incomplete_response.py` | Create | 13 |
| `tests/test_ndjson_streaming.py` | Create | 14 |
| `tests/test_version_sync.py` | Create | 16, 17 |
| `tests/test_ping_compat.py` | Create | 19 |
| `tests/test_retry.py` | Modify | 8 |
| `tests/integration/test_cli_develop.py` | Modify | 9 |
